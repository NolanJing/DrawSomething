fuser -n tcp 3000

sudo kill pid

width: 80% ##神坑
你把canvas当作一张图片就能理解了,你canvas的样式宽高改变了,但是canvas自身的宽高却是不变的,这两个宽高是不同的,两者的宽高比不同,对应的就是内容被拉伸.不想内容变形,一个是维持canvas的样式宽高比跟画布的宽高比一致,另一个就是根据样式的宽高比来重新设置画布的宽高比,这必须重新绘制内容.


【其实这里官方解释也和到位：由于是ES5 的关系，Vue.js 不能检测到对象属性的添加或者删除。因为Vue.js 在初始化实例时将属性转换为了getter/setter,所以属性必须在 data 对象上才能让Vue.js 转换他，才能让他响应。】



vue - socket.io 解决方法  开两个host服务器，一个8080  一个3000

@isLogin="isLogin" 正确
@isLogin="isLogin()" 取不到参数

iframe引入子页面与主页面的viewport设置冲突问题探讨
